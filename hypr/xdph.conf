# xdg-desktop-portal-hyprland.
#
# allow_token_by_default keeps screen-sharing pickers from re-prompting for every
# frame request within a session (browsers request a new token per share).

screencopy {
    allow_token_by_default = true
    max_fps = 60
}
