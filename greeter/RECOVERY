Neobrix greeter recovery
========================

Only needed if the login screen fails to appear after a boot. From a TTY
(Ctrl+Alt+F2) or the hypervisor console:

    sudo cp /etc/greetd/config.toml.pre-regreet /etc/greetd/config.toml
    sudo systemctl restart greetd

That switches to agreety, greetd's text-mode greeter. It is part of the greetd
package itself, so it cannot go missing.

To put the Neobrix greeter back:

    sudo cp /etc/greetd/config.toml.neobrix-greeter /etc/greetd/config.toml
    sudo systemctl restart greetd

Note: CachyOS's noctalia-greeter has been uninstalled, so it is no longer an
option. Reinstall it with `pacman -S noctalia-greeter` if you want it back.
